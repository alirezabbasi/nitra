{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "exec.fill_received.v1",
  "type": "object",
  "required": ["order_id", "venue", "canonical_symbol", "filled_notional", "event_ts"],
  "properties": {
    "order_id": {"type": "string"},
    "venue": {"type": "string"},
    "canonical_symbol": {"type": "string"},
    "filled_notional": {"type": "number"},
    "event_ts": {"type": "string"}
  }
}
